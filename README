A cool javascript interpreter that embeds Tracemonkey.

You have to compile and install the tracemonkey branch first.

You also need the readline library with headers (it's a GNU standard)

To compile the project do:
make
sudo make install
